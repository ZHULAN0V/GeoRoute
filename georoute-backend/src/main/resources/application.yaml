spring:
  application:
    name: georoute
  mvc:
    apiversion:
      use:
        path-segment: 1
      default: "1.0"

minio:
  url: "http://localhost:9000"
  access-key: minioadmin
  secret-key: minioadmin
  bucket:
    name: "routes"
